#!/bin/zsh

[[ ! -e "./.trello" ]] && exit

BOARD_ID=`cat "./.trello"`

if [[ $# = 0 ]];
then
  open "https://trello.com/b/$BOARD_ID"

else
  wrk lists in "$BOARD_ID" > /dev/null
  wrk create card in wrk1 "$@" > /dev/null
  echo Card created - \"$1\"
fi
