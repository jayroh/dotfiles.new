#!/bin/bash
# Checkout branch using fzf

set -e

if [[ $# == 0 ]]; then
   branch=$(git branch --color=always | \
       fzf --reverse --ansi --tac | \
       sed -E -e 's/^\*?[ \t]*//')
else
  branch="$1"
fi

git checkout "$branch"
